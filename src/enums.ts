export enum LocalStorageKeys {
  JWT = 'jwt',
  USER = 'user',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female'
}

export enum PageRoutes {
  REGISTER = '/register',
  LOGIN = '/login',
  HOME = '/',
}
